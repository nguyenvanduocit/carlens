# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017

# CSV Data Configuration
# Path to the directory containing CSV logs
CSV_DATA_PATH=/path/to/CsvLogs
# Vehicle ID folder name
VEHICLE_ID=YOUR_VEHICLE_ID

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Database Configuration
DATABASE_NAME=carlens
COLLECTION_NAME=vehicle_telemetry

# Security Note: 
# For production, create a .env file with your actual credentials
# and add .env to your .gitignore file to avoid committing secrets